// import 'package:flutter_html/custom_render.dart';

// class HtmlMatcher {
//   static CustomRenderMatcher videoMatcher() =>
//       (context) => context.tree.element?.localName == 'video';
//   static CustomRenderMatcher iframeMatcher() =>
//       (context) => context.tree.element?.localName == 'iframe';
//   static CustomRenderMatcher imageMatcher() =>
//       (context) => context.tree.element?.localName == 'img';

//   static CustomRenderMatcher galleryMatcher() => (context) =>
//       context.tree.element?.classes.contains('wp-block-gallery') ?? false;

//   static CustomRenderMatcher tweetMatcher() => (context) =>
//       context.tree.element?.classes.contains('twitter-tweet') ?? false;

//   static CustomRenderMatcher featuredVideoMatcher() => (context) =>
//       context.tree.element?.localName == 'iframe' ||
//       context.tree.element?.localName == 'video';
// }
